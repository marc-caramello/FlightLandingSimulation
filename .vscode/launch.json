{
    "version": "0.2.0",
    "configurations": [
        {
            "args": [],
            "cwd": "${workspaceFolder}",
            "externalConsole": true,
            "name": "Start External Program",
            "preLaunchTask": "Compile, Run, & Debug",
            "program": "${workspaceFolder}\\runThis.exe",
            "request": "launch",
            "type": "cppdbg"
        }
    ]
}